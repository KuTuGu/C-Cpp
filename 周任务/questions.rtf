git:为什么推文件时必须要有git commit？否则推不上去
3.代码最后两行存在的问题，找不出
5.为什么输出打印时一直报错Abort trap: 6
    是溢出的问题吗？可我输出的不是字符串（最后没有字符串结束标志'\0'），定义的数组20行5列，应该不会溢出吧。
7.为什么最后for的循环中不能使用n++（虽然用A[m][n]替换A［m][j]麻烦,一开始没有想到简单的方法)（添加这一行代码会输出错误结果）




