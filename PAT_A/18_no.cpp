#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int max, station, que, road;
    cin >> max >> station >> que >> road;
    vector<int> bic(station);
    for(int i = 1;i <= N;i++){
        cin >> bic[i];
    }
    for(int i = 0;i < M;i++){

    }
}